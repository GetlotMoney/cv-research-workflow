# 科研工作流 V1.0 当前验证

> 本文件只记录当前公开版本。过去的审核记录统一从 `docs/HISTORY.md` 查看。

## 验证范围

- 无个人实例时启动通用模板；
- 实例化和激活个人工作区；
- 六方向状态与 GZSL 单方向开放；
- Framework 与四类实验；
- 分支、Worktree 和 Tag 规则；
- GPU debug、正式 Run 与人工确认；
- 外置数据登记；
- 外来 Framework 标准化；
- 本机安装与 GitHub 干净发布。

## 已通过

- 根目录完整回归：`47 tests OK`；
- 科研引擎专项：`36 tests OK`，其中包括当前 Skill 说明、历史测试样本、六方向包和 Framework 真实仓库链路；
- Framework 专项：`10 tests OK`，覆盖真实 CUDA、四类实验、分支、Worktree、外来 Framework、人工确认和创新晋级；
- 六方向包：`8 tests OK`，只有 GZSL 对外开放，正式实验全部拒绝 CPU；
- 方向包文件清单：六份重新生成并逐文件校验通过；
- 安装同步：`install-research-workflow.ps1 -Check` 通过；
- 当前 Markdown 本地链接：`37 files OK`；
- 当前说明旧术语与失效链接扫描：`0 hits`；
- 统一页面：`HTTP 200`，科研标题存在，论文入口不存在；
- 独立 Agent 三场景正向复测：`PASS`，并确认当前命令全部可以显示帮助；
- `git diff --check`：通过；
- GitHub 私有发布树不包含个人实例、本机环境文件或论文系统目录。

## 当前限制

- 只有 GZSL 开放。
- 外来代码只允许运行已经人工审读的可信来源。
- 合成小数据只能证明链路可用，不能当成正式成绩。
- 系统不自动下载数据、推送远端或运行高成本训练。

本轮文档分层完成后的确切测试数字将在最终回归后更新。
