# Runner

## 负责什么

检查代码、数据、配置和 GPU 后执行 debug 或正式 Run，并保存完整运行事实。

## 需要什么

- 干净或明确标记的工作树；
- 已登记数据；
- 冻结配置与随机种子；
- `dvsr_gpu` 和可用 CUDA。

## 交付什么

- 训练、评估和推理状态；
- S、U、H；
- 日志、产物路径与哈希；
- 失败原因和清理状态。

## 不能做什么

- 不回退到 CPU；
- 不更换正式 Run 的配置；
- 不把 debug 当成正式结果。
