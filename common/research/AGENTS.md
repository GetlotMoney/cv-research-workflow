# 通用工作流模板协作入口

本仓库只保存通用工作流程序、规范、模板和测试，不保存具体项目的实验事实。

具体用户实例统一放在系统根目录的 `users/<user>/`，研究方向代码统一放在该实例的 `repositories/`。通用模板不得自动读取、继承或修改任何具体用户的项目事实。

系统级说明、流程图、审核记录和技术版本统一保存在系统根目录的 `docs/`；本目录不再维护第二套文档入口。
