# 科研工作流 V1.0 协作规则

## 唯一位置

- 系统唯一总目录是 `D:\智能双流系统`。
- 通用科研能力放在 `common/research/`。
- 个人工作区只能放在 `users/<slug>/`。
- 临时文件只能放在项目内 `.runtime/`。

## 当前范围

- 当前只实施科研工作流，不修改论文系统。
- 六个方向身份齐全，只有 GZSL 已开放。
- 当前唯一对象链是：GZSL 仓库 → Framework → 四类实验 → GPU Run → 人工确认。
- 不向用户展示历史兼容对象或历史命令。

## 科研规则

- 固定使用 Conda 环境 `dvsr_gpu` 和 CUDA，不提供 CPU 退路。
- 复现、调参、消融和创新分别编号。
- 只有创新实验绑定 Idea，只有成功并确认的创新可以晋级为子 Framework。
- 每个实验使用独立分支和 Worktree。
- 普通 Run 不打 Tag；稳定 Framework 才打 Tag。
- 不编造实验数据，不把合成调试结果当成正式成绩。
- 不自动 push、发布、下载大型数据或启动高成本训练。

## 文档规则

- README、使用说明、当前架构和 Skill 只能描述现在的 V1.0 主线。
- 历史记录必须明确标注，且不能作为当前操作说明。
- 测试输出属于测试材料，不能冒充用户文档。
- 所有正式文档放在本项目内。

## 文件保护

- 不删除有效科研仓库、真实数据、权重、日志或 Git 历史。
- 不读取或恢复旧活动副本。
- `common/paperflow/` 不在本阶段修改范围内。
