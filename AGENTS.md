# 项目协作规则

## 唯一位置

- 本系统唯一总目录是 `D:\智能双流系统`。
- 禁止在其他位置建立平行项目、副本或第二个正式入口。
- 通用能力放在 `common/`；个人实例只能由用户以后主动在 `users/` 下创建。
- 当前交付不包含任何正式个人实例；测试实例只能放在项目 `.runtime/` 并在测试后清理。

## 当前阶段

- 当前只收口科研工作流；PaperFlow 是下一阶段，本阶段不得修改 `common/paperflow/`。
- 科研和论文只通过用户主动生成的科研交付包连接，科研不会自动开始写论文。
- 六方向状态只读取 `config/directions/catalog.json`。
- 当前只有 GZSL 可运行；图像分类、目标检测、实例分割、语义分割和超分辨率必须显示为待做。

## 科研规则

- 通用科研 Skill 是 `cv-experiment-workflow`，项目源码是唯一正式来源。
- 个人 Skill 和方向仓库 Skill 只能在用户实例化个人工作区、选择方向后生成。
- GZSL 固定使用 Conda 环境 `dvsr_gpu` 和 CUDA，不提供 CPU 退路。
- 复现、调参、消融和创新四类实验分别编号；每个实验使用独立分支和 Worktree。
- 只有创新实验绑定 Idea；只有确认成功的创新才能晋级为稳定子 Framework。
- 普通 Run 不打 Tag；稳定基础框架、验证通过的外来框架和晋级子框架才打 Tag。
- 不编造实验数据，不把合成调试结果写成论文成绩。

## 文件保护

- 不删除原始论文、有效材料、知识库、正式仓库或 Git 历史。
- 不从已经清理的旧路径恢复旧入口。
- 不自动 push、发布或创建远端；必须遵守用户当前任务给出的发布范围。
